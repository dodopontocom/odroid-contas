version 2.1

jobs:
  botlint:
    machine: true
    steps:
      - checkout
      - run:
          name: "Shell Lint"
          command: |
              ls -lrt
    filters:
      branches:
        only:
          - dev-v3   

workflows:
  version: 2.1
  
  bot-workflow:
    jobs:
      - botlint
